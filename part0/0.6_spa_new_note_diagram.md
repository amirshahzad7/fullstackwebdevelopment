```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes note and clicks Save

    Note right of browser: JavaScript prevents default form submission

    browser->>server: POST /new_note_spa (JSON data)
    activate server
    Note right of server: Server saves note
    server-->>browser: 201 Created
    deactivate server

    Note right of browser: Browser updates UI without reloading page
```